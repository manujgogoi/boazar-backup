import React from "react";
import CircularProgress from "@material-ui/core/CircularProgress";

const MGCircularProgress = () => {
  return <CircularProgress />;
};

export default MGCircularProgress;
